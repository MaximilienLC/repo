FROM rocm/pytorch:rocm6.4.2_ubuntu24.04_py3.12_pytorch_release_2.6.0

RUN apt update && apt install -y swig && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir numpy einops jaxtyping datasets scikit-learn matplotlib gymnasium[classic-control,box2d] beartype ordered_set
